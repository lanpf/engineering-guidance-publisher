{
  "name": "engineering-guidance-publisher",
  "source": "https://github.com/lanpf/engineering-guidance-publisher.git",
  "version": "1.1.0",
  "agents": {
    "rules": [
      {"id": "STD-HIERARCHY-001", "text": "Shared rules apply to every project; service rules additionally apply to service projects, and project-specific authoritative documents may only add or tighten constraints."},
      {"id": "STD-DOCS-001", "text": "Before changing a service, read the project documentation entry declared by its project guidance, then read every authoritative document routed for the task."},
      {"id": "STD-DOMAIN-002", "text": "Before changing domain boundaries, language, business rules, errors, domain events, or API business semantics, read the service's authoritative domain document."},
      {"id": "JAVA-VERSION-001", "text": "Use Java 17."},
      {"id": "DEP-CYCLE-001", "text": "Module dependency cycles are forbidden; fix responsibilities or extract a shared module rather than hiding a cycle through reflection or events."},
      {"id": "ARCH-DIRECTION-001", "text": "Inner modules must not depend on outer protocols, persistence implementations, persistence objects, or the boot module."},
      {"id": "ARCH-RESPONSIBILITY-001", "text": "Keep domain rules in domain, use-case orchestration in application, technical adapters in infrastructure, protocol handling in interfaces, and runtime assembly in boot."},
      {"id": "ERROR-STABILITY-001", "text": "Never modify, reuse, or reassign a published error code."},
      {"id": "VERIFY-CHANGE-001", "text": "Run verification proportional to the change and do not claim completion without reporting the commands and results."}
    ]
  },
  "skills": [
    {
      "name": "develop-java-code",
      "when": "Creating, modifying, refactoring, or reviewing Java code",
      "references": [
        {
          "file": "java-language.md",
          "source": "standards/COMMON.md#Java 语言",
          "title": "Java Language Rules",
          "sections": [
            {
              "title": "Language and data design",
              "rules": [
                {"id": "JAVA-VERSION-002", "text": "Compile and run with Java 17."},
                {"id": "JAVA-RECORD-001", "text": "Prefer `record` for data carriers whose state is complete at construction, remains immutable, and does not require inheritance, proxies, or JavaBean setter binding."},
                {"id": "JAVA-RECORD-002", "text": "Defensively copy collection record components with `List.copyOf`, `Set.copyOf`, or the corresponding immutable-copy operation."},
                {"id": "JAVA-CLASS-001", "text": "Use an ordinary class when the type requires inheritance, mutable state, framework proxying, JavaBean shape, or complex domain behavior."},
                {"id": "JAVA-TYPE-001", "text": "Prefer wrapper types such as `Integer`, `Long`, and `Boolean`; use primitives only for intentional default values, performance characteristics, or explicitly non-null semantics."}
              ]
            },
            {
              "title": "Readable modern Java",
              "rules": [
                {"id": "JAVA-STYLE-001", "text": "Prefer `forEach` for simple collection traversal and Stream for simple transformation, filtering, and mapping."},
                {"id": "JAVA-STYLE-002", "text": "Prefer lambdas and method references for simple callbacks."},
                {"id": "JAVA-OPTIONAL-001", "text": "Use `Optional` for possibly absent return values and chained handling; do not use it as a Bean property or method parameter."},
                {"id": "JAVA-READABILITY-001", "text": "For complex business rules, nested conditions, and exception flows, prioritize readability over functional style."}
              ]
            },
            {
              "title": "Utilities",
              "rules": [
                {"id": "JAVA-UTILITY-001", "text": "Prefer the JDK, an already-used framework, or a mature maintained library over reimplementing general technical capabilities."},
                {"id": "JAVA-UTILITY-002", "text": "When Spring Framework is already present, prefer its string and collection utilities for common null, string, and collection checks."},
                {"id": "JAVA-UTILITY-003", "text": "Before adding a library, evaluate maintenance activity, security, license, and dependency cost; do not add overlapping functionality when existing dependencies suffice."}
              ]
            }
          ]
        },
        {
          "file": "validation-exceptions.md",
          "source": "standards/COMMON.md#Java 语言",
          "title": "Validation and Exception Rules",
          "sections": [
            {
              "title": "Validation",
              "rules": [
                {"id": "JAVA-VALIDATION-001", "text": "Use Jakarta Bean Validation for constraints on bindable Bean properties and method parameters."},
                {"id": "JAVA-VALIDATION-002", "text": "Use explicit checks such as `Assert` only when framework validation cannot express the constraint or an internal API requires fast failure."}
              ]
            },
            {
              "title": "Exceptions and logging",
              "rules": [
                {"id": "JAVA-EXCEPTION-001", "text": "Use framework exception types for framework, binding, and validation failures; use explicit domain exceptions for invariants and business constraints."},
                {"id": "JAVA-EXCEPTION-002", "text": "Business exceptions must belong to a defined exception base or error-code hierarchy; generic runtime exceptions must not carry business semantics."},
                {"id": "JAVA-EXCEPTION-003", "text": "Handle or propagate business exceptions; log non-business exceptions with enough context to locate the problem, and never silently ignore them."}
              ]
            }
          ]
        },
        {
          "file": "lombok.md",
          "source": "standards/COMMON.md#Lombok",
          "title": "Lombok Rules",
          "sections": [
            {
              "title": "General use",
              "rules": [
                {"id": "LOMBOK-EXPERIMENTAL-001", "text": "Do not use annotations from `lombok.experimental`."},
                {"id": "LOMBOK-BUILDER-001", "text": "Do not use `@Builder`; use explicit constructors or factory methods for staged or fluent construction."},
                {"id": "LOMBOK-CONSTRUCTOR-001", "text": "Use Lombok constructors only for direct field assignment without validation or conversion; write an explicit constructor when initialization contains invariants, transformations, or other logic."},
                {"id": "LOMBOK-LOG-001", "text": "Use `@Slf4j` instead of declaring a Logger field manually."}
              ]
            },
            {
              "title": "Type-specific use",
              "rules": [
                {"id": "LOMBOK-ENTITY-001", "text": "Aggregate roots and entities use only `@Getter`; state changes go through domain behavior, and equality based on the unique identifier is implemented explicitly without `@Data` or Lombok `@EqualsAndHashCode`."},
                {"id": "LOMBOK-VALUE-001", "text": "Prefer `record` for value objects; otherwise use final fields, getters, an appropriate all-arguments constructor, and full-field equality when suitable."},
                {"id": "LOMBOK-SPRING-001", "text": "Spring Beans use final dependencies and constructor injection, normally through `@RequiredArgsConstructor`."}
              ]
            }
          ]
        },
        {
          "file": "mapstruct.md",
          "source": "standards/COMMON.md#MapStruct",
          "title": "MapStruct Rules",
          "sections": [
            {
              "title": "Conversion contracts",
              "rules": [
                {"id": "MAPSTRUCT-CONVERSION-001", "text": "Use MapStruct for object conversion; do not hand-write field copying or use `BeanUtils.copyProperties`."},
                {"id": "MAPSTRUCT-CONTRACT-001", "text": "Define the conversion contract independently; MapStruct is an implementation mechanism rather than the contract itself."},
                {"id": "MAPSTRUCT-IMPLEMENTATION-001", "text": "Place implementations in the contract package's `mapstruct` subpackage, name them `*MapStructMapper`, implement the contract, and annotate implemented methods with `@Override`."}
              ]
            },
            {
              "title": "Reuse and configuration",
              "rules": [
                {"id": "MAPSTRUCT-REUSE-001", "text": "Reuse existing mappers through `@Mapper(uses = {...})` instead of duplicating field-level conversions."},
                {"id": "MAPSTRUCT-CONFIG-001", "text": "Declare `componentModel = spring` and `unmappedTargetPolicy = IGNORE` in a shared `@MapperConfig`; individual mappers reference that configuration."},
                {"id": "MAPSTRUCT-HELPER-001", "text": "Keep generic converters and helpers private to mapper use and do not expose them as business components."}
              ]
            }
          ]
        }
      ]
    },
    {
      "name": "manage-java-dependencies",
      "when": "Changing Maven dependencies, versions, scopes, BOMs, starters, modules, or runtime implementations",
      "references": [
        {
          "file": "dependency-rules.md",
          "source": "standards/COMMON.md#依赖规范",
          "title": "Dependency Rules",
          "sections": [
            {
              "title": "Module dependencies",
              "rules": [
                {"id": "DEP-MINIMAL-001", "text": "Each module declares only the minimum dependencies required by its current compile-time and runtime semantics."},
                {"id": "DEP-CYCLE-002", "text": "Module cycles are forbidden; remove them by extracting a shared module or correcting responsibility boundaries, never by hiding the cycle with reflection or events."},
                {"id": "DEP-SERVICE-MODULE-MANAGEMENT-001", "text": "The service parent POM must declare every service module except test-only modules and the boot assembly module under `<dependencyManagement><dependencies>`, using `${project.version}`. Do not declare those modules as direct parent-POM dependencies, because direct dependencies are inherited by child modules and would violate module dependency boundaries."},
                {"id": "DEP-STARTER-001", "text": "Only concrete technical implementations, runtime adapters, and final assembly modules may depend on aggregate starters; other modules use the smallest capability artifact."}
              ]
            },
            {
              "title": "Scopes and versions",
              "rules": [
                {"id": "DEP-SCOPE-001", "text": "Match dependency scope to use: compile requirements use compile scope, tests use test scope, and `provided` or `optional` requires a genuine runtime-provided or optional capability."},
                {"id": "DEP-SNAPSHOT-001", "text": "SNAPSHOT dependencies are forbidden in release branches and production builds and may only be temporary in local or unpublished iteration branches."},
                {"id": "DEP-VERSION-001", "text": "Manage third-party versions centrally; business and feature modules must not override versions independently."},
                {"id": "DEP-BEAN-001", "text": "Reuse infrastructure Beans supplied by the runtime framework instead of declaring another Bean with the same semantics."}
              ]
            }
          ]
        },
        {
          "file": "service-baseline.md",
          "source": "standards/SERVICE.md#技术基线",
          "title": "Service Framework and Assembly Rules",
          "sections": [
            {
              "title": "Framework baseline",
              "rules": [
                {"id": "SERVICE-SPRING-BOOT-001", "text": "Service applications use Spring Boot 3.5.16."},
                {"id": "SERVICE-SPRING-CLOUD-001", "text": "Service applications use Spring Cloud 2025.0.3."},
                {"id": "SERVICE-BOM-001", "text": "Use `framework-dependencies`, `framework-bom`, and `framework-starter-bom` for centralized versions, selecting `framework-*` contracts and `framework-starter-*` implementations as needed."},
                {"id": "SERVICE-NEUTRAL-001", "text": "Technology-neutral domain and application modules depend only on the minimum framework contracts required by their responsibility."}
              ]
            },
            {
              "title": "Runtime assembly",
              "rules": [
                {"id": "SERVICE-ASSEMBLY-001", "text": "Put replaceable persistence, messaging, and scheduling technologies in separate modules; the boot packaging dependency selects the runtime implementation."},
                {"id": "SERVICE-ASSEMBLY-002", "text": "Do not switch multiple technology implementations inside one artifact using runtime properties."},
                {"id": "SERVICE-SCHEDULER-001", "text": "Use XXL-JOB by default for fallback, compensation, and batch work; do not make it a prerequisite for the core real-time business path."}
              ]
            }
          ]
        }
      ]
    },
    {
      "name": "test-java-service",
      "when": "Creating, modifying, reviewing, or diagnosing unit, contract, or integration tests",
      "references": [
        {
          "file": "testing.md",
          "source": "standards/COMMON.md#测试规范",
          "title": "Java Testing Rules",
          "sections": [
            {
              "title": "Test boundaries",
              "rules": [
                {"id": "TEST-LOCATION-001", "text": "Keep unit and module contract tests in the tested module's `src/test`."},
                {"id": "TEST-INTEGRATION-001", "text": "Treat cross-module, full auto-configuration, real database, or real middleware scenarios as integration tests and keep them out of production modules."},
                {"id": "TEST-BEHAVIOR-001", "text": "Verify observable behavior and public contracts rather than unnecessary implementation details."},
                {"id": "TEST-MOCK-001", "text": "In unit tests, mock only injected external collaborators; do not partially mock or spy on the subject under test."},
                {"id": "TEST-CONTAINERS-001", "text": "Prefer Testcontainers for isolated and repeatable real-infrastructure scenarios."}
              ]
            },
            {
              "title": "Structure and lifecycle",
              "rules": [
                {"id": "TEST-SELF-CONTAINED-001", "text": "Keep integration scenarios self-contained; place containers, test application, configuration, probes, and lifecycle management in the corresponding `*IT` when practical."},
                {"id": "TEST-REUSE-001", "text": "Extract test support only for stable and meaningful reuse; do not create a common test base merely to remove small duplication."},
                {"id": "TEST-UNIT-NAME-001", "text": "Name unit and module contract test classes `*Test` and execute them with Maven Surefire in the test phase."},
                {"id": "TEST-IT-NAME-001", "text": "Name integration test classes `*IT` and execute them with Maven Failsafe in the integration-test and verify phases."},
                {"id": "TEST-METHOD-NAME-001", "text": "Name test methods `should<ExpectedBehavior>` to state the observable behavior directly."},
                {"id": "TEST-CLEANUP-001", "text": "Manage external infrastructure with `@Testcontainers` and `@Container` where possible, and release created application contexts and executors in `@AfterEach`."}
              ]
            },
            {
              "title": "Documentation and observation",
              "rules": [
                {"id": "TEST-DOC-001", "text": "Every integration test has same-named documentation covering its purpose, conditions, execution, manual observation steps, observation entry points, and expected result."},
                {"id": "TEST-DOC-002", "text": "The root README of a standalone test module contains only coverage scope, environment requirements, unified execution, report location, and a documentation index; detailed scenarios live in `docs/<TestClass>.md`."},
                {"id": "TEST-OBSERVABLE-001", "text": "Integration tests must allow manual observation of the key process and final result; automatic assertions or source reading alone are insufficient."}
              ]
            }
          ]
        }
      ]
    },
    {
      "name": "develop-layered-service",
      "when": "Adding service functionality, modules, contracts, domain behavior, adapters, protocols, or reviewing service architecture",
      "references": [
        {
          "file": "architecture.md",
          "source": "standards/SERVICE.md#分层结构",
          "title": "Service Architecture Rules",
          "sections": [
            {
              "title": "Service domain contract",
              "rules": [
                {"id": "SERVICE-DOC-ENTRY-001", "text": "Every service declares a project documentation entry that summarizes the project and routes tasks to authoritative documents."},
                {"id": "SERVICE-DOMAIN-DOC-003", "text": "Every service keeps an authoritative domain document defining its bounded context, domain language, business rules, error definitions, domain events, and API semantics; its path is declared by the project documentation entry and project guidance."},
                {"id": "SERVICE-DOMAIN-DOC-002", "text": "Project domain guidance may add or tighten shared rules but must not duplicate, relax, or override them."}
              ]
            },
            {
              "title": "Modules",
              "rules": [
                {"id": "ARCH-MODULE-API-001", "text": "`<service>-api` defines stable external API contracts."},
                {"id": "ARCH-MODULE-DOMAIN-001", "text": "`<service>-domain` owns the core domain model and rules."},
                {"id": "ARCH-MODULE-APPLICATION-001", "text": "`<service>-application` orchestrates write use cases and read-only queries."},
                {"id": "ARCH-MODULE-INFRA-001", "text": "`<service>-infrastructure` contains technical adapters, persistence abstractions, shared conversions, and persistence objects."},
                {"id": "ARCH-MODULE-TECH-001", "text": "Concrete persistence, scheduler, and message implementations live in `<service>-infrastructure-<kind>-<technology>` modules."},
                {"id": "ARCH-MODULE-INTERFACES-001", "text": "`<service>-interfaces` implements protocol-neutral Facades and hosts REST, RPC, and message subscription adapters."},
                {"id": "ARCH-MODULE-CLIENT-001", "text": "`<service>-openfeign-client` provides the OpenFeign client for the service API."},
                {"id": "ARCH-MODULE-BOOT-001", "text": "`<service>-boot` starts, configures, packages, and assembles runtime implementations."}
              ]
            },
            {
              "title": "Dependency direction",
              "rules": [
                {"id": "ARCH-DEP-API-001", "text": "API does not depend on business implementations; domain does not depend on other business layers; application depends on domain."},
                {"id": "ARCH-DEP-INFRA-001", "text": "Infrastructure implements domain and application ports; concrete persistence, scheduler, and message modules depend on infrastructure."},
                {"id": "ARCH-DEP-OUTER-001", "text": "Interfaces depends on API and application, OpenFeign client depends on API, and boot performs final assembly."},
                {"id": "ARCH-DEP-INWARD-001", "text": "No inner module may depend on outer protocol types, persistence objects, concrete technologies, or boot."}
              ]
            }
          ]
        },
        {
          "file": "api-domain-application.md",
          "source": "standards/SERVICE.md#层内约定",
          "title": "API, Domain, and Application Rules",
          "sections": [
            {
              "title": "API",
              "rules": [
                {"id": "API-RESPONSIBILITY-001", "text": "API defines stable Facade-centered contracts and contains interfaces and contract types without business implementations."},
                {"id": "API-CONTENTS-001", "text": "API may contain command, query, response, event, enum, and constants packages; enums represent finite values and constants represent headers, topics, event types, and similar fixed values."},
                {"id": "API-FACADE-001", "text": "Facade inputs implement `Request` and are complete, immutable API command/query records independent of HTTP binding and header injection; results are `BaseResult` implementations, using `PageResult<T>` for paged collections and `Result<T>` otherwise, including `Result<Void>` for no payload and `Result<List<T>>` for non-paged collections."},
                {"id": "API-RESPONSE-001", "text": "API response payloads use records consistently."},
                {"id": "API-EVENT-001", "text": "API integration events implement `IntegrationEvent`; they may be mapped from domain events but must use distinct types."}
              ]
            },
            {
              "title": "Domain",
              "rules": [
                {"id": "DOMAIN-MODEL-001", "text": "Use aggregate roots as consistency boundaries and define entities, value objects, domain services, repository contracts, and domain events around the domain model."},
                {"id": "DOMAIN-BEHAVIOR-001", "text": "Aggregate roots and entities change state only through domain behavior; value objects are immutable; domain services contain only cross-aggregate rules or rules that do not belong to one aggregate."},
                {"id": "DOMAIN-CLOCK-001", "text": "Domain objects and services must not call system time directly; application services inject `Clock`, obtain business time once per use case, and pass the same value to related state changes and events."},
                {"id": "DOMAIN-REPOSITORY-001", "text": "Domain repositories express domain-object persistence contracts without exposing a persistence technology."},
                {"id": "DOMAIN-EVENT-001", "text": "Domain events represent facts that already occurred and receive a trend-increasing Long event ID at creation."},
                {"id": "DOMAIN-BOUNDARY-001", "text": "Domain must not know API payloads, application commands/outputs/views, persistence DOs, concrete technologies, `Result<T>`, or `PageResult<T>`."}
              ]
            },
            {
              "title": "Application",
              "rules": [
                {"id": "APPLICATION-RESPONSIBILITY-001", "text": "Application orchestrates use cases, coordinates domain objects and ports, and does not implement domain rules."},
                {"id": "APPLICATION-COMMAND-001", "text": "Command services are write-use-case entry points, accept application commands, return command outputs or void, and own transaction boundaries."},
                {"id": "APPLICATION-QUERY-001", "text": "Query services are read-only, accept domain IDs, value objects, or query conditions, return application views, and use `PagedList<T>` for pagination."},
                {"id": "APPLICATION-EVENT-001", "text": "Submit integration events through an `IntegrationEventOutbox` port rather than binding application code to a message broker."},
                {"id": "APPLICATION-BOUNDARY-001", "text": "Application may depend on domain, API integration-event contracts, and framework ports, but not API command/query/response types, protocol implementations, persistence DOs, or external response wrappers."}
              ]
            }
          ]
        },
        {
          "file": "infrastructure-interfaces-boot.md",
          "source": "standards/SERVICE.md#层内约定",
          "title": "Infrastructure, Interfaces, Client, and Boot Rules",
          "sections": [
            {
              "title": "Infrastructure and configuration",
              "rules": [
                {"id": "INFRA-RESPONSIBILITY-001", "text": "Infrastructure provides repository, gateway, ID, event storage, messaging, scheduling, and other technical adapters without domain rules or use-case orchestration."},
                {"id": "INFRA-CONFIG-001", "text": "For JavaBean configuration binding, collection and nested-object fields are final, expose getters without setters, and begin as empty binding containers without business defaults."},
                {"id": "INFRA-CONFIG-002", "text": "Required configuration collections use `@NotEmpty` so missing configuration fails startup rather than running silently with an empty value."},
                {"id": "INFRA-DURATION-001", "text": "A standalone minimum for `Duration` uses both `@NotNull` and Hibernate Validator `@DurationMin` with explicit units; the module declares `hibernate-validator` directly."},
                {"id": "INFRA-CROSS-FIELD-001", "text": "Use `@AssertTrue` and similar type-level validation only for relationships between fields."}
              ]
            },
            {
              "title": "Persistence and asynchronous adapters",
              "rules": [
                {"id": "INFRA-REPOSITORY-001", "text": "Repository adapters implement domain repositories or application ports; persistence repositories expose data-access capability only."},
                {"id": "INFRA-PERSISTENCE-001", "text": "Keep concrete repository implementations, SQL XML, and technical assembly in the matching persistence implementation module; the packaged dependency determines the runtime implementation."},
                {"id": "INFRA-DO-001", "text": "Shared persistence models live in infrastructure; DOs declare only necessary entity, identifier, and enum mapping annotations, while table constraints and indexes live in schema SQL."},
                {"id": "INFRA-MAPPER-001", "text": "Mappers convert domain objects, application read models, and persistence objects; generic helpers remain mapper-only and technology-neutral."},
                {"id": "INFRA-QUERY-001", "text": "Name persistence queries by conditions, range, and ordering rather than by a single caller or use case."},
                {"id": "INFRA-ASYNC-001", "text": "Domain event storage, outbox, and partitioned messaging follow framework ports; scheduler modules expose task entries while compensation logic remains in shared infrastructure services."}
              ]
            },
            {
              "title": "Interfaces, client, and boot",
              "rules": [
                {"id": "INTERFACES-RESPONSIBILITY-001", "text": "Interfaces provides protocol-neutral Facade implementations and REST, RPC, and message entry points without domain rules or use-case orchestration."},
                {"id": "INTERFACES-FACADE-001", "text": "Facade implementations convert API/application objects, call application services, and wrap responses with `Result<T>` or `PageResult<T>`."},
                {"id": "INTERFACES-REST-001", "text": "Controllers handle routing, binding, and necessary protocol context only, convert the HTTP request into an API command/query, then invoke a Facade contract."},
                {"id": "INTERFACES-HEADER-CONTEXT-001", "text": "Inject or resolve Client, Channel, Device, and other HTTP header context only in interfaces; API, application, and domain must not depend on the HTTP header binding mechanism."},
                {"id": "INTERFACES-REST-CONTEXT-001", "text": "After the gateway supplies client context, each controller business request is one mutable `@Valid` HTTP request class extending `ClientRequest`, or `ClientChannelRequest` only when channel context is required; `ClientRequest` requires `clientAppId`, while `ClientChannelRequest` additionally requires `channelCode`. Do not split business fields into path or query parameters alongside a separate client-context argument. For endpoints without a business body, declare the concrete request-context type and let the shared argument resolver build and validate it from protected headers."},
                {"id": "INTERFACES-FACADE-BOUNDARY-001", "text": "After header injection and validation, interfaces converts its mutable HTTP request into a complete immutable API command/query record; Facades must not accept interfaces HTTP request types."},
                {"id": "INTERFACES-RPC-001", "text": "Publish the same Facade Bean directly when the RPC technology supports it; add a technology-specific RPC adapter only for incompatible models, semantics, metadata, or exception handling."},
                {"id": "INTERFACES-MESSAGE-001", "text": "Message listeners convert inbound contracts and invoke application services without being required to pass through a Facade."},
                {"id": "CLIENT-FEIGN-001", "text": "OpenFeign clients match Facade method signatures, reuse API contract types and response wrappers, and contain no business model, use case, or conversion rules."},
                {"id": "BOOT-RESPONSIBILITY-001", "text": "Boot owns startup, runtime configuration, implementation selection, and packaging; absent implementation-module dependencies mean the capability is unavailable at runtime."},
                {"id": "BOOT-BOUNDARY-001", "text": "Boot contains no domain rules, application orchestration, protocol adapters, or business types."}
              ]
            }
          ]
        },
        {
          "file": "data-carriers.md",
          "source": "standards/SERVICE.md#不可变数据载体",
          "title": "Layered Data Carrier Rules",
          "sections": [
            {
              "title": "API and domain",
              "rules": [
                {"id": "DATA-API-001", "text": "Prefer records for API commands and queries, and always use records for API responses; these contracts are complete, immutable, and independent of HTTP binding and header injection. Use records for API events when serialization, RPC, OpenFeign, and client compatibility support immutable constructor binding."},
                {"id": "DATA-API-COMPAT-001", "text": "For public Java APIs, evaluate the binary-constructor compatibility impact before adding a record component."},
                {"id": "DATA-DOMAIN-001", "text": "Prefer records for domain IDs, value objects, outcomes, and immutable domain events when inheritance is unnecessary; use classes for aggregate roots, entities, framework bases, mutable state, and complex behavior."},
                {"id": "DATA-INTERFACES-001", "text": "Use ordinary mutable classes for interfaces HTTP requests so JSON binding and Client, Channel, or Device header injection can populate them; they may extend the required Client context type but must not be used as Facade inputs."}
              ]
            },
            {
              "title": "Application and persistence",
              "rules": [
                {"id": "DATA-APPLICATION-001", "text": "Prefer records for immutable application commands, command outputs, query conditions, and views; services, repositories, gateways, and mappers remain classes."},
                {"id": "DATA-PERSISTENCE-001", "text": "Read-only persistence projections may be records; DOs, JPA entities, and no-argument or setter-bound types remain classes."}
              ]
            }
          ]
        },
        {
          "file": "naming.md",
          "source": "standards/SERVICE.md#命名约定",
          "title": "Service Naming Rules",
          "sections": [
            {
              "title": "General names",
              "rules": [
                {"id": "NAME-INTERFACE-001", "text": "Name interfaces by business role, port, or technical contract without `I*` or `*Interface`; use `Abstract*` for abstract classes and reserve `Base*` for framework or shared base types."},
                {"id": "NAME-IMPLEMENTATION-001", "text": "Do not use generic `*Impl`; name implementations by default role, technology, adapter responsibility, or strategy."},
                {"id": "NAME-RESULT-001", "text": "Do not suffix payloads, command outputs, views, or outcomes with `*Result`; reserve `Result<T>` and `PageResult<T>` for framework response wrappers."},
                {"id": "NAME-MAPPER-001", "text": "Name mapper contracts by layer responsibility, place MapStruct implementations in the contract package's `mapstruct` subpackage, and name them `*MapStructMapper`."}
              ]
            },
            {
              "title": "Layer names",
              "rules": [
                {"id": "NAME-API-001", "text": "Use `*ApiCommand`, `*ApiCommandResponse`, `*ApiQuery`, `*ApiQueryResponse`, `*ApiEnum`, `*ApiConstants`, `*ApiEvent`, and `*Facade` in API."},
                {"id": "NAME-DOMAIN-001", "text": "Use `*Outcome` for domain-service results, `*Repository` for repository contracts, and `*Event` for domain events."},
                {"id": "NAME-APPLICATION-001", "text": "Use `*Command`, `*CommandOutput`, `*CommandService`, `*View`, and `*QueryService` in application; paged queries return `PagedList<*View>`."},
                {"id": "NAME-INFRA-001", "text": "Use `*RepositoryAdapter`, technology-specific `*PersistenceRepository`, `*DO`, and `*PersistenceConfiguration` in infrastructure."},
                {"id": "NAME-INTERFACES-001", "text": "Place protocol-neutral Facade implementations in `interfaces.facade` as `Default*Facade`; use `*RpcAdapter` only for necessary technology-specific RPC adapters."},
                {"id": "NAME-CLIENT-001", "text": "Name OpenFeign clients `*FeignClient`."},
                {"id": "NAME-BOOT-001", "text": "Do not define business types in the boot module."}
              ]
            }
          ]
        }
      ]
    },
    {
      "name": "manage-service-error-codes",
      "when": "Allocating, changing, documenting, or reviewing service error codes",
      "references": [
        {
          "file": "error-codes.md",
          "source": "standards/SERVICE.md#错误码",
          "title": "Service Error Code Rules",
          "sections": [
            {
              "title": "Code structure",
              "rules": [
                {"id": "ERROR-FORMAT-001", "text": "A complete error code has six digits: a three-digit service prefix plus a three-digit local code; error enums declare only the local code, and the framework composes and zero-pads the full code."},
                {"id": "ERROR-CONTEXT-001", "text": "One service project represents one bounded context and uses one unique service prefix; independently deployed bounded contexts require distinct prefixes."},
                {"id": "ERROR-DOMAIN-001", "text": "Allocate local codes `000-599` to `DomainError`: reserve `000-099` for shared domain errors and partition `100-599` into aligned 50-code blocks. Each aggregate is assigned one or more adjacent complete blocks according to its required capacity."},
                {"id": "ERROR-DOMAIN-CAPACITY-001", "text": "A 50-code block is the minimum allocation unit for aggregate errors. An aggregate exclusively owns every code in all of its assigned blocks and may use one or multiple adjacent blocks. Capacity expansion must reserve additional adjacent, currently unallocated complete blocks; blocks must not be split, shared, or reassigned between aggregates."},
                {"id": "ERROR-APPLICATION-001", "text": "Allocate local codes `600-699` to `ApplicationError` and prefix names with `APP_`."},
                {"id": "ERROR-INFRA-001", "text": "Allocate local codes `700-899` to `InfrastructureError`."},
                {"id": "ERROR-INFRA-TECH-001", "text": "Reserve `700-799` for failures of technical mechanisms owned by the service, such as local persistence, caching, locking, serialization, files, event storage, and outbox processing; prefix names with `INFRA_TECH_`."},
                {"id": "ERROR-INFRA-ACL-001", "text": "Reserve `800-899` for failures at external integration and anti-corruption boundaries, such as remote transport, authentication, protocol, unavailable providers, malformed responses, and provider-specific rejections that cannot be translated into local business semantics; prefix names with `INFRA_ACL_`."},
                {"id": "ERROR-INFRA-TRANSLATION-001", "text": "Translate an external rejection into `DomainError` or `ApplicationError` when it has a stable local business meaning; do not classify it as infrastructure solely because it originated from another system."},
                {"id": "ERROR-RESERVED-001", "text": "Keep local codes `900-999` reserved and unallocated."}
              ]
            },
            {
              "title": "Domain allocation",
              "rules": [
                {"id": "ERROR-DOMAIN-COMMON-001", "text": "Prefix shared domain errors with `DOMAIN_`; the first two are `DOMAIN_ENTITY_ID_INVALID` and `DOMAIN_EVENT_ID_REQUIRED`."},
                {"id": "ERROR-AGGREGATE-001", "text": "Prefix aggregate errors with the aggregate name; the first two are `{AGGREGATE}_NOT_FOUND` and `{AGGREGATE}_ALREADY_EXISTS`."},
                {"id": "ERROR-RANGE-001", "text": "Unallocated complete aggregate ranges remain available, but unused codes inside an assigned aggregate range remain owned by that aggregate and must not be reassigned."},
                {"id": "ERROR-DOCUMENTATION-002", "text": "Document the service prefix, shared-domain range, and every aggregate range in the authoritative domain document declared by the project documentation entry."}
              ]
            },
            {
              "title": "Stability",
              "rules": [
                {"id": "ERROR-APPEND-001", "text": "Append new errors in local-code order within the owning range."},
                {"id": "ERROR-STABILITY-002", "text": "Published error codes are immutable and must not be modified, reused, or assigned to a different meaning."}
              ]
            }
          ]
        }
      ]
    },
    {
      "name": "update-engineering-standards",
      "when": "Reconciling changes in COMMON.md or SERVICE.md with catalog.json and preparing a standards release",
      "references": []
    },
    {
      "name": "sync-engineering-standards",
      "when": "Installing or upgrading generated engineering guidance in one named project",
      "references": []
    }
  ]
}
